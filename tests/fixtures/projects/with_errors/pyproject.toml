[build-system]
requires = ["nuwa-build"]
build-backend = "nuwa_build"

[project]
name = "with-errors"
version = "0.1.0"

[tool.nuwa]
nim-source = "nim"
module-name = "with_errors"
lib-name = "with_errors_lib"
entry-point = "with_errors_lib.nim"
nimble-deps = ["nimpy", "nuwa_sdk"]
